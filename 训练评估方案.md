# 训练评估方案

> 适用项目：`TTS-and-VoiceDesign`、`TTS-Studio-WebUI`  
> 评估对象：悬疑/恐怖故事生成、A→B 有声演播文本改写、现有导演层与 TTS 工作流  
> 方案版本：1.0（2026-08-26）

## 1. 结论

最终目标不是“训练一个会写恐怖文本的模型”，也不是“多训练一个 LoRA”，而是降低从素材到成品音频的端到端失败率，同时保持剧情事实、角色关系和现有工程契约不变。

推荐方案如下：

1. 使用同一个 Qwen3.5 基座，分别训练两个可独立加载的 LoRA：
   - `story-writer-lora`：题材、梗概、约束 → 文学原稿 A。
   - `audio-polisher-lora`：原稿 A 或生硬译稿 → 可直接送入导演层的纯文本 B。
2. 不让 A→B LoRA 生成 Unitale JSON、情绪标签、音效、BGM 或 TTS 参数；现有导演层继续负责 B→C。
3. A→B 采用“模型 + 确定性处理”的混合方案：
   - LoRA 学习语义保真、口语节奏、对白归属、指代消歧和悬念节奏。
   - 脚本处理数字/日期/缩写读法、禁用标签、长度、字符和 JSON/Schema 校验。
4. 不把市面悬疑音频的 ASR 转写作为核心 SFT 数据。ASR 文本最多用于已获授权素材的辅助评测，或未来“导演/韵律层”研究。
5. 先做提示词基线，再做 300～500 条人工确认的 A→B Gold POC；只有微调在固定盲测中显著胜过基线，才扩充到 1,000～3,000 条。
6. Easy Dataset 用于解析、辅助生成、审校、确认和评测；LLaMA-Factory 才负责训练。两者之间必须增加“成对数据装配”步骤，确保每条训练样本都显式包含 A 和 B。

这里有一个必须特别强调的实现陷阱：Easy Dataset 当前的一键 LLaMA-Factory 导出默认只导出“问题→答案”，不会把生成答案时使用的原始文本块 A 自动放进用户消息。如果直接训练，模型学到的是“按固定问题生成 B”，而不是“A→B 改写”。因此，一键导出不能未经检查就进入训练。

## 2. 第一性原理拆解

### 2.1 真正的优化目标

系统最终交付的是音频，而不是文本。可用成品需同时满足：

- 故事成立：情节、因果、时间线、人物身份和关键揭示正确。
- 文本可演：句子长度、停连、指代、对白归属和口语节奏适合朗读。
- 导演可编排：现有导演层能稳定生成符合项目契约的结构化结果。
- TTS 可合成：不因异常符号、过长片段、错误角色或资源竞争失败。
- 工程可复现：同一版本、同一参数、同一测试集可以重复比较。

因此总目标可以表示为：

`总成功率 = 故事正确率 × A→B 成功率 × 导演成功率 × TTS 成功率`

任一环节存在硬错误，成品就失败。单独追求“文字更有恐怖味”不能替代端到端验收。

### 2.2 四层产物边界

| 层 | 输入 → 输出 | 负责内容 | 明确不负责 |
|---|---|---|---|
| A：文学原稿 | 题材/梗概 → A | 情节、氛围、悬念、人物、文学表达 | TTS 标签、Unitale JSON、声线参数 |
| B：演播文本 | A/生硬译稿 → B | 语义保真、口语化、节奏、对白归属、指代消歧 | 情绪曲线 JSON、SFX、BGM、音色分配 |
| C：导演结果 | B → C | 角色、分段、情绪、SFX/BGM、Unitale 项目结构 | 改写故事事实 |
| D：音频 | C → D | TTS 合成、混音、资源产出 | 重新解释剧情 |

现有项目已经有 B→C→D 的实现，因此本轮训练应只补齐 A→B，不重复训练已有导演层。

### 2.3 为什么不让 LoRA 包办所有规则

“更适合朗读”是开放式判断，适合由模型学习；“不得输出 Markdown”“数字必须规范化”“JSON 必须通过 Schema”是确定性约束，脚本能做到可解释、零随机和稳定回归。

如果把两类问题都压给 LoRA，有限的数据容量会被格式规则占用，而且模型仍可能偶发漏规则。最优分工是：

- 模型负责不能用简单规则表达的变换。
- 脚本负责能被明确验证的变换。
- 评测同时覆盖文本层和最终音频层。

## 3. 两种 A→B 数据路线比较

### 3.1 方案一：用期刊/短篇小说构造 A→B 配对

做法：把合法可用的悬疑期刊、短篇恐怖小说或自有稿件作为 A，由高质量教师模型生成候选 B，再由人工逐条对照、修订和确认。

优点：

- A 与 B 天然可以一一对应，直接形成监督信号。
- 可明确控制“保持剧情不变、只做演播化”的任务边界。
- 可覆盖项目真实输入中的长句、书面语、指代、对白和悬念结构。
- 容易建立故事级训练/验证/测试隔离。
- 方便针对当前导演层和 TTS 的真实失败样本做增量训练。

缺点：

- B 需要高质量教师模型和人工审校，数据成本较高。
- 期刊和小说通常有版权，必须确认训练、内部使用和分发权限。
- 如果 B 全由同一个教师模型生成，可能继承教师的固定文风和错误。

结论：这是核心训练数据的推荐路线，但必须保证权利清晰和人工 Gold 审核。

### 3.2 方案二：悬疑/恐怖音频 → ASR 文本

做法：下载或采集悬疑音频，用 ASR 得到转写文本，整理后用于训练。

优点：

- 能采集真实口语中的停连、重复、口头表达和对白形式。
- 如果同时拥有音频、原文、时间戳、说话人和韵律标注，未来可用于导演层或表现力评测。

缺点：

- 单独 ASR 只得到 B，没有对应 A，不能监督 A→B 的“变换”。
- 很多有声书只是照读原文，A≈B，几乎没有可学习的改写信号。
- ASR 会丢失音高、强弱、情绪和停顿等声音信息，并引入标点、同音字、说话人错误。
- 市面音频通常存在录音、表演、文本和平台条款等多重权利风险。
- 若用 ASR 文本训练语言模型，模型可能反而学到识别错误和口水化表达。

结论：不作为核心 A→B SFT 数据。只有在音频已获授权且能取得对应原稿时，才把“原稿 A—人工校对演播转写 B—音频”三联数据用于辅助研究。

### 3.3 最终选择

核心路线采用“合法文本 A + 教师模型候选 B + 人工 Gold B”。授权音频只作为以下两类补充：

- 评测 B 经 TTS 合成后是否更自然。
- 未来训练或评估 C 层的说话人、韵律、停顿和情绪决策。

## 4. 开放平台数据集判断

当前能找到的公开数据多属于以下类别：

- 中文网文或文学文本：能提供风格语料，但没有同内容的演播改写 B。
- 中文 TTS/有声书音文对：提供“音频—转写”，目标是语音合成或评测，不提供 A→B 改写配对。
- 中文通用指令数据：可增强通用指令遵循，但与悬疑演播改写不匹配。
- 多人对话/ASR 数据：可帮助语音或对话任务，但不包含文学稿到演播稿的监督。

例如，LLaMA-Factory 官方列出的中文 WebNovel 是文学文本而非 A→B 配对；OpenDialog 是大规模中英文口语对话音频；ContextTTS 是中文会话/有声书音文评测数据。这些可以作为弱数据、灵感或评测补充，但不能替代本项目的 Gold 配对。

因此，开放平台目前没有可直接用于本任务、同时满足“中文悬疑/恐怖 + 原稿 A + 演播改写 B + 权利清晰”的现成核心数据集。下载前必须逐个检查数据卡、许可证、来源链和商业使用限制，不能只看平台页面上的 License 标签。

## 5. LoRA 架构决策

### 5.1 两个 LoRA，而不是一个混合 LoRA

| LoRA | 输入 | 输出 | 核心能力 |
|---|---|---|---|
| `story-writer-lora` | 题材、人物、梗概、篇幅、禁项 | A | 悬疑/恐怖叙事、情节和氛围 |
| `audio-polisher-lora` | A 或生硬译稿 | B | 语义保真、演播口语化、对白和节奏 |

两者使用完全相同的基座型号和版本，但训练数据、系统提示词、输出目录和评测集完全分离。生产时按顺序切换适配器：先生成 A，再将 A 交给 A→B 适配器。POC 阶段不同时激活、相加或合并两个 LoRA，避免能力干扰和故障定位困难。

### 5.2 为什么先训练 A→B

即使暂时不训练作者 LoRA，也可以用已有小说、翻译稿或人工稿作为 A，直接验证 B 是否提升最终音频。这样能隔离变量，最快证明新增工序是否有价值。

只有 A→B 达到 Gold 门槛后，再训练 `story-writer-lora`。否则两个新模型同时变化，无法判断最终音频改善或退化来自哪一层。

### 5.3 基座选择

- POC 推荐 `Qwen/Qwen3.5-4B` 的原始 Hugging Face Transformers 权重，4-bit QLoRA。
- 如果生产基座必须是 Qwen3.5-9B，则 LoRA 也必须在完全相同的 9B 基座版本上训练；4B LoRA 不能挂到 9B。
- Ollama/GGUF/int4 成品量化文件不能直接作为 LLaMA-Factory 的 QLoRA 基座。训练使用原始 Transformers 权重，验收后再做部署格式转换。
- 4070 Ti Super 16GB 上，4B 是低风险 POC；9B 的可行上下文长度需实测，不能预设一定能装下完整长篇配对。

## 6. 数据设计

### 6.1 每条 A→B 样本必须长这样

```json
{
  "messages": [
    {
      "role": "system",
      "content": "你是中文有声演播文本润色器。保持事实、人物、因果和时间线不变，只输出可演播纯文本。"
    },
    {
      "role": "user",
      "content": "请将以下原稿改写为适合中文悬疑有声演播的纯文本。\n\n<原稿>\n这里必须出现原稿 A\n</原稿>"
    },
    {
      "role": "assistant",
      "content": "这里是逐条人工确认的演播文本 B"
    }
  ]
}
```

不可接受的样本包括：

- 用户消息只有“请改写为演播稿”，却没有 A。
- Assistant 输出包含分析、思维链、Markdown、JSON、角色标签或音效标签。
- A 和 B 来自不同故事或无法追溯配对关系。
- B 改变凶手、人物身份、关键线索、时间线或结局。
- 同一故事的片段同时出现在训练集和测试集。

### 6.2 数据规模

| 阶段 | 建议规模 | 目的 |
|---|---:|---|
| 提示词基线 | 80～120 个 Gold 测试片段 | 证明是否真的需要训练 |
| POC SFT | 300～500 对 Gold A→B | 验证 LoRA 是否稳定提升 |
| 第一版生产 | 1,000～3,000 对 Gold/高银样本 | 扩展题材、长度和失败类型 |
| 后续迭代 | 只加入真实失败闭环样本 | 避免盲目堆数据 |

少量高质量配对优于大量未经确认的合成数据。POC 至少覆盖 20～30 个完整故事来源，不能把一个故事切成几百段后当作几百个独立样本。

### 6.3 切分原则

- 以完整故事或完整来源为单位做 train/validation/test 划分，推荐 80/10/10。
- 同一故事的所有场景必须进入同一 split。
- 测试集在任何训练、提示词调优和人工回填中保持封存。
- 对近重复、转载、改名版本做去重；按段随机切分会造成严重泄漏。
- 若单条 A+B 超过训练上下文，按“完整场景/完整揭示单元”切分，并保留稳定的 `story_id` 和必要的前情摘要。

### 6.4 B 的内容规范

B 必须：

- 保留所有关键事件、事实、人物关系、因果和揭示顺序。
- 让长句、倒装和书面连接自然适合朗读。
- 消除会让听众误解的指代，明确必要的说话人，但不机械重复姓名。
- 保持悬疑停顿和信息释放节奏，不过度解释。
- 只输出中文演播正文。

B 不得包含：

- Unitale JSON、Markdown、代码块、表格。
- `<think>`、分析过程或质量自评。
- `[旁白]`、`[SFX]`、`[BGM]`、情绪/音色/TTS 参数等导演标签。
- 新增人物、线索、动机、场景或结局。

## 7. Easy Dataset 与 LLaMA-Factory 的职责边界

Easy Dataset 当前支持 PDF、Markdown、DOCX、TXT、EPUB 处理、文本切分、自定义提示词、问题模板、数据审校、盲测以及 Alpaca/ShareGPT 导出。它是数据工程工具，不执行 LoRA 训练。

LLaMA-Factory 负责：

- 加载 Qwen3.5 原始基座。
- 读取 ShareGPT/OpenAI `messages` 数据。
- 执行 SFT + 4-bit QLoRA。
- 保存/加载 LoRA，提供 CLI、WebChat 或 OpenAI 风格 API 进行验证。
- 在不使用量化基座的前提下合并 LoRA。

强制中间工序是：

`Easy Dataset 已确认数据 + 原始块 A → 成对数据装配/校验 → ShareGPT A→B → LLaMA-Factory`

## 8. 评测方案

### 8.1 固定基线

比较对象：

1. 原始基座 + 最佳固定提示词。
2. `audio-polisher-lora` + 同一提示词。
3. 可选：外部教师模型，仅作参考上限。

固定以下变量：

- 测试集和顺序。
- 基座版本、模板和推理参数。
- temperature、top_p、seed、最大输出长度。
- 导演提示词与程序版本。
- TTS 模型、参考音频、音色、分段和采样参数。
- 第一轮音频对比关闭 SFX/BGM，第二轮再验证完整链路。

### 8.2 技术通过与 Gold 通过分离

技术通过只表示程序可运行，例如文件可解析、JSON 合法、TTS 成功；Gold 通过表示内容可交付。技术通过不能抵消内容硬错误。

任何一项以下错误都令该样本 Gold 失败：

- 关键事件缺失或改变。
- 新增事实、人物、动机或线索。
- 凶手/身份/时间线/因果错误。
- 对白归属或指代导致角色混淆。
- 输出分析、JSON、标签或思维链。
- 导演结果不能通过项目现有 Schema/契约。
- TTS 因文本异常而无法产出。

### 8.3 建议门槛

| 指标 | POC 门槛 |
|---|---:|
| Gold 关键事件召回 | 100% |
| 事实精确率/无新增事实 | 100% |
| 角色与对白归属硬错误 | 0 |
| 禁用格式/标签出现 | 0 |
| 导演 JSON 解析与 Schema 通过 | 100% |
| TTS 合成成功率 | ≥98% |
| 长度/字符/规则校验通过率 | ≥95%，其余可由脚本自动修复 |
| 至少 30 条人工盲测中，LoRA 相对基线偏好率 | ≥65% |
| 相对基线的语义硬错误 | 不得增加 |

如果关键事件召回是 97.3%，即使平均分很高，也仍可能包含多个不可交付样本，不能视为 Gold 通过。

### 8.4 端到端验收

每个候选 B 需经过：

1. 纯文本静态校验。
2. 现有导演层 B→C。
3. Unitale 项目结构和 `schemaVersion: 4` 契约校验。
4. 固定 TTS 后端合成。
5. 人工盲听自然度、角色清晰度、悬念节奏和语义一致性。

不改动 `audioAssetKey`、`bgmLibrary`、`project.currentState`、`assets` 等现有字段，也不让训练模型直接生成这些字段。

## 9. 工程与资源约束

- 训练环境与 TTS 环境隔离；训练期间停止所有占用 GPU 的 TTS/LLM 工作进程。
- 延续现有单重模型、一次性 worker 和 `GPU_LOCK_FILE` 原则，不能靠删除锁文件绕过仍在运行的进程。
- 训练使用独立虚拟环境和独立模型目录，不在 TTS 项目运行时安装依赖。
- 生产仍遵守 `LOCAL_FILES_ONLY=1`、`uv run --no-sync` 和禁止运行时下载/安装的约束。
- 记录 LLaMA-Factory commit、基座模型 revision、数据 manifest、训练 YAML、随机种子和输出 adapter hash。
- 先用 4B/8K 做 POC；OOM 时按 8192→6144→4096 降低上下文，再考虑场景切分。不要先用降到 2-bit 的方式换取容量。

## 10. 分阶段实施与停止条件

### 阶段 0：无训练基线

- 制作 80～120 条封存 Gold 测试集。
- 用 Qwen3.5 基座 + 固定提示词生成 B。
- 完成文本、导演、TTS、盲听评测。

停止条件：如果基线已经达到所有门槛，不训练 LoRA，只保留提示词和规则层。

### 阶段 1：A→B POC

- 制作 300～500 条已确认配对。
- 训练 Qwen3.5-4B 4-bit QLoRA。
- 与阶段 0 在同一测试集盲比。

停止条件：如果偏好率不足 65%、语义硬错误增加，或提升仅来自更长/更夸张的文本，回到数据规范，不扩大规模。

### 阶段 2：生产候选

- 扩充到 1,000～3,000 条，重点补真实失败类型。
- 在 4B 与目标生产基座之间做成本/质量比较。
- 完成完整 B→C→D 回归。

停止条件：任何现有项目契约退化、GPU 工作流冲突或封存测试集泄漏，均不得接入默认流程。

### 阶段 3：作者 LoRA

- 单独构造“梗概/约束→A”数据和测试集。
- 训练 `story-writer-lora`，不混入 A→B 样本。
- 最终按“作者→润色→导演→TTS”整链验收。

## 11. 决策清单

- [x] A→B 单独建模，但不是全部交给 LoRA。
- [x] 作者与润色使用两个独立 LoRA。
- [x] B 保持纯文本，导演层契约不变。
- [x] ASR 音频不进入核心 SFT 数据。
- [x] 开放平台数据仅作弱数据或评测补充。
- [x] Easy Dataset 一键导出前必须验证 A 是否显式存在。
- [x] 故事级切分、封存 Gold 测试和端到端验收是上线前置条件。
- [x] 先基线、后 POC、通过后扩量。

## 12. 主要参考

- [Easy Dataset 官方仓库](https://github.com/ConardLi/easy-dataset)
- [Easy Dataset 当前 LLaMA-Factory 导出实现](https://github.com/ConardLi/easy-dataset/blob/main/app/api/projects/%5BprojectId%5D/llamaFactory/generate/route.js)
- [Easy Dataset 问题模板实现](https://github.com/ConardLi/easy-dataset/blob/main/lib/services/questions/template.js)
- [LLaMA-Factory 官方仓库](https://github.com/hiyouga/LlamaFactory)
- [LLaMA-Factory 数据格式说明](https://github.com/hiyouga/LlamaFactory/blob/main/data/README.md)
- [LLaMA-Factory 训练、推理与合并示例](https://github.com/hiyouga/LlamaFactory/blob/main/examples/README.md)
- [LLaMA-Factory Qwen3.5 官方示例](https://github.com/hiyouga/LlamaFactory/blob/main/examples/ascend/qwen3_5_full_sft_fsdp2.yaml)
- [Qwen3.5-4B 官方模型卡](https://huggingface.co/Qwen/Qwen3.5-4B)
- [OpenDialog 数据卡](https://huggingface.co/datasets/k2-fsa/OpenDialog)
- [ContextTTS 评测数据卡](https://huggingface.co/datasets/rodenhhh/ContextTTS_dataset)

