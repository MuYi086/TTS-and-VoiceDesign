# 有声书文本润色 LoRA 微调步骤操作文档

## 1. 当前结论

本轮已经完整跑通一次小样本流程：从 `scoring-for-TTS/training/悚族` 以固定随机种子抽取
10 个 Markdown 来源，每个来源在标题边界内取 3 个片段，由本机 `qwen3.5:9b` 生成 A/B
候选，再按来源隔离为 24 条训练、3 条验证、3 条测试数据。候选已导入 Easy Dataset，
随后用 LLaMA-Factory 对本机 Qwen3-4B-Instruct-2507 完成 BF16 LoRA 训练，并对同一隔离
测试集执行训练前后推理。

这只是验证链路和观察方向的 POC，不是生产模型。30 条 B 均为自动教师生成，状态固定为
`automated-poc`，Easy Dataset 中保持“未确认”；原始杂志文本的授权状态未得到证明，状态固定为
`research-only`。在人工逐条校对和权利确认前，不得称为 Gold 数据，不得发布 adapter，不得用于
生产或商业用途。

实跑结果：

| 项目 | 结果 |
| --- | --- |
| Easy Dataset | 项目 `audio-polisher-lora-poc`，10 个来源，30 条未确认候选，失败 0 |
| 烟测 | 1 step 成功，train loss `0.2687`，eval loss `0.2832` |
| 正式 POC | 24 train / 3 validation，3 epoch，18 step，耗时 `52.49s` |
| 最终 loss | train `0.257488`，validation `0.083817` |
| 隔离测试 | 3 条，基线与 LoRA 均为纯文本 3/3 |
| 字符相似度代理指标 | 基线 `0.928933`，LoRA `0.974670`，差值 `+0.045737` |

字符相似度只衡量输出接近本轮自动教师 B 的程度，不等于语义正确率、听感、泛化能力或生产质量。
3 条测试数据也不足以作统计结论；它只能说明这次训练产生了方向一致且可检测的变化。

## 2. 任务边界

完整产品链路应拆成独立阶段：

```text
A 原始小说文本
  -> 本文训练的 audio-polisher LoRA
B 纯净、可直接朗读的文本
  -> TTS-Studio-WebUI 现有导演/编排逻辑
C Unitale 分镜、角色、情绪、音效和 BGM JSON
  -> TTS-and-VoiceDesign 现有本地服务
D 语音、音效、BGM 与空间母带
```

本 LoRA 只学习 A → B：修复明显 OCR 错字、错误断行和标点，改善长句停顿与对白可读性，同时
保持人物、事实、情节、视角和信息量。B 必须是纯正文，不得包含 `<think>`、Markdown 代码块、
JSON、`Unitale` 字段、情绪标签、`[SFX]`、`[BGM]`、解释、总结或续写。

不要把 B → C 并入同一个 LoRA。WebUI 已负责导演 JSON，后端已负责语音合成和音频生成；本轮
不新增后端接口，也不改变已有路由。

## 3. 参考资料与仓库分工

本操作流程基于以下内容整理并实跑：

- 本仓库的 `README.md`、`模型微调学习路径.md`、
  `Easy Dataset 与 LLaMA-Factory 训练操作指南.md`。
- `TTS-Studio-WebUI` 的 README、项目约束和 A/B/C/D 现有职责。
- Easy Dataset 的 README、Prisma schema、文件上传和数据导入实现。
- LLaMA-Factory 的本地 README、数据格式说明、Qwen3 LoRA 与预测示例。
- 用户提供的完整共享对话：
  `https://chatgpt.com/share/6a9150cd-c0bc-83ec-a454-a8b91dcf14d9`。

关键分工：Easy Dataset 用于查看来源、审核 A/B 和确认 Gold；LLaMA-Factory 用于训练。Easy
Dataset 的通用问答导出不能自动理解“来源片段 A”这一业务语义，所以导入时把“任务说明 + A”
完整放入 `question`，把 B 放入 `answer`；训练 JSON 则由本仓库脚本直接生成，避免字段丢失。

## 4. 本次环境与版本

实跑日期为 2026-08-28，环境如下：

| 组件 | 实跑值 |
| --- | --- |
| Python | `3.12.13` |
| uv | `0.12.3` |
| Node.js | `v24.15.0` |
| GPU | NVIDIA GeForce RTX 4070 Ti SUPER，16376 MiB |
| Driver | `610.47` |
| LLaMA-Factory | `0.9.6.dev0`，commit `a18110d2f064b1518ac313eeb2ba980946467b4d` |
| torch | `2.13.0` |
| transformers | `5.8.0` |
| datasets | `4.0.0` |
| peft | `0.18.1` |
| accelerate | `1.11.0` |
| Easy Dataset | commit `4002b09d9c5726cafb9f61a8d12765cb96a2d94b` |
| scoring-for-TTS | commit `64e6d6f4dfcc2ec119e00236eec3dd5d02aec1ad` |

原指南建议先用 Qwen3.5-4B 加 4-bit QLoRA。本机当时没有完整 Transformers 格式的
Qwen3.5-4B 权重，shell 网络也不可用；Ollama 模型不能当作可训练权重。LLaMA-Factory 环境又没有
`bitsandbytes`。因此本轮明确改用已有的完整 Qwen3-4B-Instruct-2507 权重，执行 BF16 LoRA。
这是离线 POC 的受控替代，不应写成原方案已经按 Qwen3.5/QLoRA 完成。

## 5. 目录和产物

先定义路径；不要把路径写进 Python 源码：

```bash
workspace_root=~/github
tts_root="${workspace_root}/TTS-and-VoiceDesign"
source_dir="${workspace_root}/scoring-for-TTS/training/悚族"
easy_root="${workspace_root}/easy-dataset"
llamafactory_root="${workspace_root}/LlamaFactory"
model_path=~/hf-mirror/Qwen/Qwen3-4B-Instruct-2507
run_dir="${tts_root}/storage/training/audio-polisher-poc"
```

跟踪进 Git 的可复现入口：

- `scripts/training/build_audio_polisher_poc.py`：抽样、清洗、切片、教师生成、划分和写数据。
- `scripts/training/import_audio_polisher_to_easy_dataset.py`：幂等创建项目、上传来源、导入候选。
- `scripts/training/run_audio_polisher_inference.py`：基础模型/LoRA 的固定测试集推理。
- `scripts/training/compare_audio_polisher_predictions.py`：格式与字符级代理指标。
- `training/configs/audio_polisher_qwen3_4b_lora_poc.yaml`：本次训练配置。
- `training/configs/audio_polisher_qwen3_4b_predict_poc.yaml`：依赖齐全时可用的 LLaMA-Factory
  批量预测配置。
- `tests/test_audio_polisher_poc.py`：全部无模型回归测试。

实际数据、原文片段、预测和 adapter 都位于 `storage/training/audio-polisher-poc/`。`storage/`
已有忽略规则，这些内容不会进入 Git：

```text
data/
  dataset_info.json
  generation_resume.json
  manifest.json
  story_audio_polish_train.json
  story_audio_polish_validation.json
  story_audio_polish_test.json
easy_dataset_import.json
smoke/
adapter/
predictions/base/generated_predictions.jsonl
predictions/adapter/generated_predictions.jsonl
predictions/comparison.json
```

## 6. 从零复现

### 6.1 前置检查

```bash
cd "${tts_root}"
test -d "${source_dir}"
test -d "${easy_root}"
test -x "${llamafactory_root}/.venv/bin/llamafactory-cli"
test -f "${model_path}/config.json"
python --version
uv --version
nvidia-smi
ollama list
```

必须确认来源授权。无法确认时只能保留 `research-only`，所有运行产物只能留在本机忽略目录。
不得提交原始 Markdown、生成数据、模型权重、adapter、数据库、密钥或日志。

### 6.2 启动 Easy Dataset

首次在本机运行时，`npx prisma db push` 曾尝试联网下载 Prisma engine。缓存里已有匹配版本，使用
显式 engine 路径后可完全离线初始化：

```bash
cd "${easy_root}"
schema_engine_path=~/.cache/prisma/master/c2990dca591cba766e3b7ef5d9e8a84796e47ab7/debian-openssl-3.0.x/schema-engine
query_engine_path="${easy_root}/node_modules/.pnpm/@prisma+engines@6.19.3/node_modules/@prisma/engines/libquery_engine-debian-openssl-3.0.x.so.node"

test -x "${schema_engine_path}"
test -f "${query_engine_path}"
PRISMA_SCHEMA_ENGINE_BINARY="${schema_engine_path}" \
PRISMA_QUERY_ENGINE_LIBRARY="${query_engine_path}" \
npx prisma db push

PRISMA_QUERY_ENGINE_LIBRARY="${query_engine_path}" npx next dev -p 1717
```

另开终端验证：

```bash
curl -fsS http://127.0.0.1:1717/api/projects
```

如果依赖或 Prisma 版本变化，不要盲用上面的缓存路径；先读取 `package.json` 和 lockfile，找到版本
匹配的 engine。禁止把本机 engine 复制进仓库。

### 6.3 生成 10 来源 / 30 条候选

先确认 Ollama 服务和教师模型可用：

```bash
curl -fsS http://127.0.0.1:11434/api/tags >/dev/null
ollama show qwen3.5:9b >/dev/null
```

再构建数据：

```bash
cd "${tts_root}"
uv run --project qa --locked python scripts/training/build_audio_polisher_poc.py \
  --source-dir "${source_dir}" \
  --output-dir "${run_dir}" \
  --story-count 10 \
  --samples-per-story 3 \
  --teacher-model qwen3.5:9b \
  --seed 42
```

脚本先按文件名排序，再用种子 42 随机抽 10 个有效 `.md`，所以相同来源集合下可复现。它会删除
Markdown 图片、纯 URL 和常见 OCR 英文占位文本；切片不跨 `#` 标题边界，默认约 320～960 字，
并排除目录、广告、星座、编辑互动等标题。每个来源取内部等分位置的 3 条，避开杂志卷首和卷尾。

教师调用使用低温度、固定种子和 `think: false`。输出必须通过纯文本、禁止标记和长度比例检查；
每成功一条就原子更新 `generation_resume.json`，中断后用相同命令会复用已完成结果。

### 6.4 审计划分

训练/验证/测试必须按完整来源分组，不能先切片再随机分条。检查：

```bash
cd "${tts_root}"
uv run --project qa --locked python - <<'PY'
import json
from pathlib import Path

data_dir = Path("storage/training/audio-polisher-poc/data")
seen_sources = set()
for split in ("train", "validation", "test"):
    rows = json.loads((data_dir / f"story_audio_polish_{split}.json").read_text())
    sources = {row["metadata"]["source_file"] for row in rows}
    assert not seen_sources.intersection(sources)
    assert all(row["metadata"]["review_status"] == "automated-poc" for row in rows)
    assert all(row["metadata"]["rights_status"] == "research-only" for row in rows)
    seen_sources.update(sources)
    print(split, len(rows), len(sources))
PY
```

预期为 `train 24 8`、`validation 3 1`、`test 3 1`。

### 6.5 导入 Easy Dataset 并人工审核

```bash
cd "${tts_root}"
uv run --project qa --locked python \
  -m scripts.training.import_audio_polisher_to_easy_dataset \
  --source-dir "${source_dir}" \
  --output-dir "${run_dir}"
```

首次运行会创建 `audio-polisher-lora-poc`、上传 10 个原始 Markdown，并导入 30 条未确认候选。
二次运行在条数恰好为 30 时只检查并跳过；若数据库里是 1～29 条或多于 30 条，脚本会停止，避免
重复导入。

打开 `http://127.0.0.1:1717`，逐条对照 A/B。人工检查至少包括：

- 人物、事实、事件顺序、视角和对白归属是否保持。
- 是否修复了 OCR 断行、错字和标点，而不是擅自改写剧情。
- 是否添加了情绪、音效、BGM、解释、总结、标题或续写。
- 生僻字、人名、多音字和引用符号是否适合实际 TTS。
- A/B 是否包含隐私、违法内容或无授权材料。

只有人工检查并确认的条目才可以提升为 Gold。本 POC 为了验证流程，训练使用的是自动候选；这项
例外必须保留在实验记录中，不得沿用到生产训练。

### 6.6 生成训练前基线

先释放 Ollama 占用的 GPU：

```bash
ollama stop qwen3.5:9b
```

本机 LLaMA-Factory 的 `predict_with_generate` 会强制检查 `jieba`、`nltk` 和
`rouge_chinese`。当前离线环境只有缓存的 `jieba` wheel，另外两个包不存在，因此实际基线使用
同一 LLaMA-Factory 虚拟环境里的 Transformers 推理入口：

```bash
cd "${tts_root}"
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=0 \
"${llamafactory_root}/.venv/bin/python" \
  -m scripts.training.run_audio_polisher_inference \
  --model-path "${model_path}" \
  --dataset "${run_dir}/data/story_audio_polish_test.json" \
  --output "${run_dir}/predictions/base/generated_predictions.jsonl" \
  --max-new-tokens 1024
```

该脚本使用 `enable_thinking=False`、贪心解码和同一 system/user 模板，并移除行末 Markdown 硬
换行空格。不要在训练前后更换 prompt、解码参数或测试集。

如果三个 NLG 包已按 LLaMA-Factory 的依赖规范安装，也可以运行跟踪的 predict YAML：

```bash
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=0 \
"${llamafactory_root}/.venv/bin/llamafactory-cli" train \
  training/configs/audio_polisher_qwen3_4b_predict_poc.yaml \
  model_name_or_path="${model_path}" \
  output_dir="${run_dir}/predictions/base-lf"
```

### 6.7 先跑 1-step 显存烟测

```bash
cd "${tts_root}"
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=0 \
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
"${llamafactory_root}/.venv/bin/llamafactory-cli" train \
  training/configs/audio_polisher_qwen3_4b_lora_poc.yaml \
  model_name_or_path="${model_path}" \
  output_dir="${run_dir}/smoke" \
  max_samples=2 \
  max_steps=1 \
  eval_strategy=steps \
  eval_steps=100 \
  save_strategy=steps \
  save_steps=100 \
  plot_loss=false
```

不要在 OmegaConf 命令行覆盖中直接写 `eval_strategy=no`；当前版本会把 YAML 的 `no` 解析为
布尔 `False`，随后被 Transformers 拒绝。上面的 100-step 间隔对只有 1 step 的烟测等价于不做
周期性 eval/save；LLaMA-Factory 仍可能在训练结束执行一次最终评估和保存。

只有烟测成功且没有 OOM，才运行完整训练。若 OOM，依次降低 `cutoff_len`、减少 LoRA target、
启用更激进的 checkpointing，或在安装 `bitsandbytes` 后改 4-bit QLoRA；不要一开始就任意改多个
变量。

### 6.8 运行完整 POC LoRA

```bash
cd "${tts_root}"
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=0 \
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
"${llamafactory_root}/.venv/bin/llamafactory-cli" train \
  training/configs/audio_polisher_qwen3_4b_lora_poc.yaml \
  model_name_or_path="${model_path}"
```

主要训练参数：

| 参数 | 值 |
| --- | --- |
| 模板 | `qwen3_nothink`，`enable_thinking: false` |
| cutoff | 2048 |
| LoRA | rank 8，alpha 16，dropout 0.05，target `all` |
| 数值精度 | BF16，不量化 |
| batch | per-device 1，gradient accumulation 4 |
| 优化器 | `adamw_torch` |
| learning rate | `1e-4`，cosine，warmup 2 step |
| 训练轮次 | 3 |
| 显存措施 | gradient checkpointing、expandable segments |
| 复现 | seed/data_seed 42 |

训练结束至少检查：

```bash
test -f "${run_dir}/adapter/adapter_config.json"
test -f "${run_dir}/adapter/adapter_model.safetensors"
test -f "${run_dir}/adapter/train_results.json"
test -f "${run_dir}/adapter/eval_results.json"
```

### 6.9 训练后推理与比较

```bash
cd "${tts_root}"
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=0 \
"${llamafactory_root}/.venv/bin/python" \
  -m scripts.training.run_audio_polisher_inference \
  --model-path "${model_path}" \
  --adapter-path "${run_dir}/adapter" \
  --dataset "${run_dir}/data/story_audio_polish_test.json" \
  --output "${run_dir}/predictions/adapter/generated_predictions.jsonl" \
  --max-new-tokens 1024

uv run --project qa --locked python \
  -m scripts.training.compare_audio_polisher_predictions \
  --baseline "${run_dir}/predictions/base/generated_predictions.jsonl" \
  --adapter "${run_dir}/predictions/adapter/generated_predictions.jsonl" \
  --output "${run_dir}/predictions/comparison.json"
```

除代理指标外，必须盲审训练前后输出。至少记录：事实错误率、人物/对白归属错误率、OCR 修复率、
标点与断句可朗读率、非法格式率、过度改写率，以及送入目标 TTS 后的听感。不能只看 train loss。

## 7. 本次固定随机抽样与隔离结果

种子 42 的抽样和分组如下；SHA-256 完整值保存在运行目录的 `data/manifest.json`：

| split | 来源 | 样本数 |
| --- | --- | ---: |
| validation | `《悚族》凶宅买卖.md` | 3 |
| train | `《悚族》AB轻文学系列-美丽诅咒.md` | 3 |
| test | `《悚族》美人植物馆.md` | 3 |
| train | `《悚族》回头无路.md` | 3 |
| train | `《悚族》千叶镇怪谈.md` | 3 |
| train | `《悚族特辑》退魔师.md` | 3 |
| train | `《悚族》书写你的死期.md` | 3 |
| train | `《悚族特辑》童谣夜唱.md` | 3 |
| train | `《悚族》不能说的秘密.md` | 3 |
| train | `《悚族》第二颗纽扣.md` | 3 |

每个 `.md` 实际是 OCR 后的一期杂志合集，不一定是一篇单独故事。以整期为 split group 是保守的
防泄漏策略；正式数据应增加 `work_id`、`chapter_id` 和授权字段，并按作品或系列隔离，而不能只靠
文件名。

## 8. 实跑记录与校验值

本次运行目录的关键校验值：

| 文件 | SHA-256 / 大小 |
| --- | --- |
| `data/manifest.json` | `9c6ce7f14108c644e482f249a5179d3a878202cb992ba26bd761d0ebbaaaf39b` |
| `adapter/adapter_model.safetensors` | `32dfedae340244b0b1ce1224e00caca3e54e84218575e012ed1ae14ca70e9d3e`，66126768 bytes |
| `predictions/comparison.json` | `64b7a8c58b25432da377e88b28241c6b42c40e35fa334003930c1e3e3e238faa` |

训练参数统计：可训练参数 16,515,072，总参数 4,038,983,168，可训练占比 0.4089%。验证 loss
按 epoch 为 `0.1545 → 0.09091 → 0.08382`。最终测试代理指标为：

| 指标 | 基线 | LoRA |
| --- | ---: | ---: |
| 纯文本通过 | 3/3 | 3/3 |
| 教师标签平均字符相似度 | 0.928933 | 0.974670 |
| 教师标签平均长度比例 | 0.968598 | 0.992317 |

推理后额外检查 LoRA 输出的行末空白为 0，禁止标记为 0。

## 9. 升级到原计划的 Qwen3.5 + QLoRA

满足以下条件后再升级：

1. 获得完整 Transformers 格式的 Qwen3.5-4B 权重，不能使用 Ollama blob 代替。
2. 按 LLaMA-Factory 当前 lock/安装规范补齐 `bitsandbytes`，确认 CUDA 与 torch 版本兼容。
3. LLaMA-Factory 中确认相应模板名称和 `enable_thinking` 行为；当前目标应使用 no-think 模板。
4. 先重跑不带 adapter 的 Qwen3.5 基线，不能拿 Qwen3 基线与 Qwen3.5 LoRA 横向比较。
5. 在训练配置中只切换以下项目：
   - `model_name_or_path` 改为 Qwen3.5-4B 完整权重。
   - 模板改为该版本实际支持的 `qwen3_5_nothink`。
   - 增加 `quantization_bit: 4` 和当前 LLaMA-Factory 支持的 bitsandbytes 量化设置。
6. 仍先跑 1 step 烟测，再运行完整训练，并重新记录环境、显存、hash 和测试结果。

不要在未验证模板和权重格式时只改模型名。Qwen3 adapter 也不能直接挂到 Qwen3.5 基础模型上。

## 10. 从 POC 到生产的最低门槛

建议至少完成以下工作后，才讨论接入 WebUI：

- 建立 300～500 条以上经人工双人复核的 Gold A/B；数量不是唯一目标，质量和覆盖更重要。
- 建立独立、冻结、按作品隔离的验证集和测试集，测试集建议至少 50 条且覆盖不同文体和 OCR 问题。
- 每条记录保留 `source_id`、作品/章节、授权、哈希、编辑人、复核人、状态和版本。
- 明确定义允许修改和禁止修改的标注手册，并统计审核者一致性。
- 用规则指标、语义审核、人工盲审和真实 TTS 听感共同评估。
- 与无 LoRA 基线、提示词基线和更大模型进行相同测试集对照。
- 检查灾难性过改、续写、人物关系变化、敏感信息和格式泄漏。
- 只有权利明确的数据与 adapter 才能进入发布流程。

接入时仍保持 A → B 独立：WebUI 调用文本润色后把 B 送入现有 B → C 导演逻辑；失败时应可回退
到未经润色的 A 或人工编辑的 B。不要让 LoRA 直接生成 Unitale JSON，也不要修改本仓库已经确定的
语音、音效、BGM 或空间音频接口。

## 11. 常见故障

### Prisma engine 下载卡住

症状：Easy Dataset 启动前 `prisma db push` 长时间等待网络。先核对 Prisma 版本，再设置本机缓存
的 `PRISMA_SCHEMA_ENGINE_BINARY` 和 `PRISMA_QUERY_ENGINE_LIBRARY`。不要从未知版本复制 engine。

### LLaMA-Factory 批量预测报缺少 jieba/nltk/rouge_chinese

这是 `predict_with_generate` 的 NLG 指标依赖检查，不是模型损坏。联网且允许改环境时按
LLaMA-Factory 的安装规范补齐；离线时使用本仓库的 Transformers + PEFT 推理脚本。不要创建空壳
包绕过检查。

### `False is not a valid IntervalStrategy`

OmegaConf 把命令行 `no` 解析成布尔值。使用配置文件中的字符串，或像烟测命令一样改成超出总
step 的 `steps` 间隔。

### CUDA OOM

先确认 Ollama 和其他模型进程已释放 GPU，再看序列长度。保持 batch 1 和 gradient checkpointing，
然后单变量降低 `cutoff_len`；具备依赖后再切 4-bit QLoRA。不要删除异常日志或把 OOM 写成成功。

### 输出出现 JSON、思考、SFX/BGM 或标题

该样本不能进入 Gold。检查 system prompt、no-think 模板、`enable_thinking=False` 和数据标签。
构建器会拒绝明显非法教师输出，推理比较器也会统计常见禁止标记，但人工审核仍不可省略。

### Easy Dataset 重复数据

使用幂等导入脚本。若现有数量既不是 0 也不是预期 30，脚本会停止；先在 UI 和 SQLite 中核对，
不要直接再次 POST 导入。

## 12. 最终验证命令

代码或文档变化后先跑专项测试：

```bash
cd "${tts_root}"
uv run --project qa --locked ruff check scripts/training tests/test_audio_polisher_poc.py
uv run --project qa --locked ruff format --check scripts/training tests/test_audio_polisher_poc.py
uv run --project qa --locked python -m unittest tests.test_audio_polisher_poc -v
```

交付前执行仓库统一质量门禁：

```bash
bash -n start.sh
bash scripts/quality_gate.sh
```

最后检查 Git，确保只有脚本、配置、测试和本操作文档进入变更，`storage/`、Easy Dataset SQLite、
原始来源和 adapter 都没有被提交：

```bash
git status --short
git diff --check
```
