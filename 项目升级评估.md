# TTS 与音色设计项目升级评估

> 评估对象：`~/github/TTS-and-VoiceDesign`、`~/github/scoring-for-TTS` 以及其实际调用方 `~/github/TTS-Studio-WebUI`  
> 评估日期：2026-08-13  
> 目标：在不破坏现有 TTS、音色设计、SoundEffect 和评测能力的前提下，引入 `pyproject.toml`、`uv`、`uv.lock` 与 `ruff` 等现代 Python 项目基础设施。

> 当前状态补充（2026-08-15）：MOSS-SoundEffect 已完成 uv 迁移，8311 仅由
> `moss_soundEffect/` 提供；原 SoundEffect API、worker 和
> `moss-soundEffect` Conda 环境已删除。本文其余
> “升级前”描述保留作为历史评估记录。

## 一、结论摘要

建议升级，并采用“控制面现代化、模型运行面按模型边界治理”的分层方案：

1. `TTS-and-VoiceDesign` 的 HTTP 包装器、共享校验逻辑、测试与开发工具使用一个 Python 3.12 的 `uv` 项目管理。
2. VoxCPM2、MOSS、Stable Audio 等模型的推理环境继续独立使用 Conda；Qwen、Step-Audio-EditX、LongCat 和 dots.tts-soar 已分别迁移到独立 uv 项目，不把不同模型强行合并到一个环境。
3. `scoring-for-TTS` 使用自己的 `pyproject.toml` 和 `uv.lock` 管理评测脚本、测试和轻量依赖；CUDA 模型环境仍由各模型安装指南或独立环境清单管理。
4. `start.sh`、端口、接口路径、请求字段、WAV 响应、GPU 锁和“一请求一 worker”生命周期都必须保留。升级应先增加新入口，再切换默认入口，确保可以回退到现有启动方式。

不建议一次性把两个仓库重构成一个可发布的 Python 包，也不建议把所有模型依赖写进同一个 `pyproject.toml`。这会把本来已经隔离的 PyTorch、CUDA、Transformers、Flash Attention 和上游源码依赖重新混在一起，反而降低可复现性和可维护性。

## 二、教程要点及其适用边界

本次先阅读了任务指定的两篇教程：

- [pyproject.toml + uv](https://learn-py.org/lessons/python23/pyproject-uv/)
- [ruff：一站式代码检查](https://learn-py.org/lessons/python24/ruff/)

教程中适合本项目的原则是：

- 用 PEP 621 的 `[project]` 集中声明项目名称、Python 版本和轻量运行依赖。
- 用 `[dependency-groups]` 区分开发、测试和检查工具。
- 用 `uv.lock` 锁定传递依赖，用 `.python-version` 固定控制面 Python 版本。
- 用 `uv sync` 和 `uv run` 代替手工激活虚拟环境、手工维护未锁定的依赖列表。
- 用 `ruff check`、`ruff format --check` 统一导入、常见错误和格式检查；Ruff 不能替代真正的类型检查器，也不应未经基线确认就格式化整个供应商目录。

这些原则主要解决 Python 控制面的问题。它们不能自动解决模型权重、CUDA 驱动、NVIDIA 架构、PyTorch 二进制、Flash Attention、上游 Git 源码和本地 Hugging Face 权重目录的问题。因此本项目应采用“双层依赖治理”，而不是“一个工具包打天下”。

## 三、当前状态核对

### 3.1 `TTS-and-VoiceDesign`

当前仓库是以脚本和 HTTP 包装器为中心的本地 GPU 后端：

- 当前没有 `pyproject.toml`、`uv.lock`、`requirements.txt`、`environment.yml` 或 `environment.yaml`。
- `start.sh` 启动 7 个 HTTP 进程：主 API、SoundEffect、Stable Audio 3 Medium、Qwen3-TTS、VoxCPM2、LongCat-AudioDiT 和 dots.tts-soar。
- 主服务端口为 `8300`；专用服务端口为 `8305`、`8306`、`8307`、`8308`、`8311`、`8313`，其中 `8306` 只承载 VoxCPM2。
- HTTP 包装器只导入 FastAPI、Pydantic、音频和进程管理等轻量依赖；模型推理通过各模型自己的 uv/Conda 运行时启动一次性 worker。worker 退出后释放显存，所有 worker 通过 `GPU_LOCK_FILE` 串行化。
- 当前 `.gitignore` 已忽略 `__pycache__/`、`.cache/`、`.venv/`、`.cnb/`、`.cnb.yml`、模型/供应商目录、上传音频和生成音频。
- 当前工作区没有实际的 `tests/` 目录；只有 `soundEffect/test_moss_soundeffect_v2.py` 这一类 GPU/模型相关 smoke test，不适合作为默认无模型回归测试。
- `.cnb` 目录和 `.cnb.yml` 文件当前都不存在；`start.sh` 也不依赖它们。它们不影响服务启动。`__pycache__` 只属于 Python 缓存，删除后会自动重建；`storage/clone/`、`storage/timbre/` 和运行缓存则属于运行数据，不能和 Python 缓存一并处理。

这说明后端需要补齐项目元数据和无模型测试，但不需要先重写模型 worker。

### 3.2 `scoring-for-TTS`

该仓库已经有初步现代化配置：

- `pyproject.toml` 声明项目名 `timbre-design`、Python `>=3.11`、Hatch 构建后端、pytest 开发依赖和 Ruff 行宽 100。
- 但是它同时声明了 `src/timbre_design` 和 `timbre_design.cli:main`，当前工作区没有 `src/`、`src/timbre_design/`、`README.md` 或 `uv.lock`。这是一份不完整的包配置，不能直接据此宣称项目已经可 `uv sync`、可安装或可执行。
- 仓库实际主体仍是 `modelScript/`、`task-runner/`、冻结评测文本/报告和 `tests/`；它更像评测应用与脚本集合，而不是已经完成的可发布 Python 包。
- 当前 Conda 的 `audio_eval` 环境可以运行 pytest，但在仓库根目录运行全量测试得到 65 个通过、17 个失败。失败主要来自测试引用的 `tts-bench/`、`cloneData/`、`modelScript/tts_local_higgs_audio_v3_tts_4b.py` 等当前缺失路径，以及一处任务文档契约不一致。迁移前必须先区分“代码回归失败”和“测试/资产缺失”，不能用一次 `uv sync` 掩盖它们。

对 `scoring-for-TTS` 的第一步不是继续堆依赖，而是决定它的真实边界：如果暂时没有 `timbre_design` 源码，就应按应用/脚本项目管理；只有在补齐该包后，才保留 Hatch 的 package 和 CLI 声明。

### 3.3 当前 Conda 环境矩阵

本机已核对到以下环境和 Python 版本：

| 用途 | 当前环境 | Python | 是否由 `start.sh` 直接使用 |
| --- | --- | ---: | --- |
| 主 API 包装器、默认 Qwen3-TTS 包装器 | `qwen3_tts/.venv` | 3.12.13 | 是，由 `start.sh` 的 uv 控制面启动 |
| Qwen3-TTS VoiceDesign worker | `qwen3-voiceDesign` | 3.12.13 | 否，由主 API 拉起 |
| MOSS VoiceGenerator | `moss_voiceGenerator/.venv` | 3.12.13 | 是，由 `start.sh` 启动 8315 |
| Step-Audio-EditX worker | `Step-Audio-EditX` | 3.12.13 | 否，由主 API 拉起 |
| MOSS-SoundEffect worker | `moss_soundEffect/.venv` | 3.12.13 | 是，由 `8311` uv API 拉起 |
| VoxCPM2 worker | `voxcpm2` | 3.10.20 | 否，由 `8306` API 拉起 |
| LongCat-AudioDiT worker | `LongCat_AudioDiT_3.5B_bf16/.venv` | 3.12.13 | 是，由 `start.sh` 启动 8307 |
| dots.tts-soar worker | `dots_tts_soar/.venv` | 3.12.13 | 是，由 `start.sh` 启动 8308 |
| Stable Audio 3 Medium API/worker | `stable_audio_3_medium/.venv` | 3.12.13 | 是，由 `start.sh` 启动 8313 |
| scoring 评测环境 | `audio_eval` | 3.10.20 | 不属于后端启动链 |
| MOSS Audio 4B Thinking 评测环境 | `moss-audio-4b-thinking` | 已存在，未作为后端服务启动 | 不属于后端启动链 |

Python 3.10 与 3.12 的并存是模型现实约束，不是项目治理失败。升级后应记录并验证它，而不是强制统一到一个 Python 版本。

## 四、升级前后对比

| 维度 | 升级前 | 建议升级后 | 收益 | 代价/风险 |
| --- | --- | --- | --- | --- |
| 项目元数据 | 后端没有标准项目配置；评测仓库配置不完整 | 两个仓库各自拥有清晰的 `pyproject.toml` | 新人可从一个入口理解项目 | 需要先清理虚假包声明和依赖边界 |
| 依赖复现 | 主要依赖 Conda 环境、安装指南和机器状态 | 控制面/评测依赖用 `uv.lock`；模型环境独立锁定 | 降低“我电脑能跑”的概率 | GPU 依赖仍需要 Conda、CUDA 和上游版本验证 |
| 虚拟环境 | `start.sh` 默认在 `qwen3-tts` 内启动全部包装器 | 已迁移服务使用各自项目 `.venv`；其余模型继续 `conda run` | 包装器启动更轻、更明确 | 需要确认各模型 uv 项目的 GPU 依赖 |
| 代码质量 | 没有后端测试目录和统一检查入口 | Ruff 只检查自有源码，测试覆盖 HTTP 契约和 worker 边界 | 更早发现导入、未定义名称和接口回归 | 老代码首次 lint 可能有较多历史问题 |
| 服务契约 | 依赖 README、WebUI 和实际代码共同维持 | 用固定 JSON fixture、FastAPI schema 和契约测试守住兼容性 | 前端无需跟随内部重构 | fixture 需要持续维护 |
| 模型生命周期 | 一请求一 worker、共享 GPU 锁、进程退出释放显存 | 完全保留，并增加环境预检和日志 | 保留当前显存安全边界 | 不能直接享受常驻模型的低延迟 |
| 启动方式 | `bash start.sh` | `bash start.sh` 继续可用，内部逐步改为项目运行时 | 现有部署和 WebUI 不必改动 | 迁移期间要维护旧/新两条启动路径 |
| 供应商代码和权重 | 本地目录、缓存、模型权重不进 Git | 继续外置，用环境变量和清单描述 | 避免仓库膨胀和泄露机器路径 | 部署者仍需准备本地模型资产 |

## 五、推荐目标结构

当前控制面使用 `main/` 的平铺导入方式，降低导入路径和 subprocess 参数的风险：

```text
TTS-and-VoiceDesign/
├── pyproject.toml              # API 控制面依赖、Ruff、测试入口
├── uv.lock                     # 只锁控制面依赖
├── .python-version             # 建议 3.12
├── start.sh                    # 保留兼容入口，逐步调用项目 Python
├── main/                        # 8300 控制面和运行时辅助模块
├── tests/                       # 无模型 unittest 契约测试
├── envs/                        # 每个模型 Conda 环境的可审查清单
├── scripts/                    # health、契约、环境预检脚本
├── soundEffect/                # GPU smoke test，默认不纳入无模型测试
└── docs/                       # 接口、环境和迁移说明
```

`scoring-for-TTS` 建议先按应用项目整理：

```text
scoring-for-TTS/
├── pyproject.toml              # 评测/测试/检查依赖
├── uv.lock
├── .python-version             # 建议 3.11，和现有 pyproject 对齐
├── modelScript/
├── task-runner/
├── tests/
├── envs/                       # audio_eval 及各模型环境说明
└── docs/
```

如果后续确实需要 `timbre_design` CLI，再单独创建 `src/timbre_design/`，并在测试证明包存在后恢复 Hatch package 和 `[project.scripts]`。不要保留一个指向不存在源码的可安装包声明。

## 六、详细升级步骤

### 阶段 0：冻结现有行为和建立基线

1. 为两个仓库分别记录当前 commit、工作区状态、Conda 环境名、Python 版本、模型路径变量和启动命令。不要把当前用户的未提交改动混入迁移提交。
2. 从 `README.md`、`start.sh`、各 `*_api.py` 和 `TTS-Studio-WebUI/docs/TTS-and-VoiceDesign接入.md` 生成一份接口矩阵。
3. 为每个 HTTP 服务记录：健康检查、上传参考音频、音频哈希检查、合成/生成、音色设计、编辑、错误状态和响应 Content-Type。
4. 先修正或明确现有评测测试引用的缺失路径。当前 `scoring-for-TTS` 的 17 个失败必须在迁移前标成“待修复基线”或恢复所需 fixture，不能直接把失败归因于 uv。
5. 把 `soundEffectResult/` 中的请求、旧结果和文本作为契约回归样本，而不是运行时输入。`resultDrama.json` 当前只有 8 个 `sfx_plan`，可用于复现“覆盖不足”的基线；`deepseekResponse.json` 的 `choices[0].message.content` 为空且以长度上限结束，不能当作一次成功的严格 JSON 输出。

### 阶段 1：先现代化后端控制面

1. 新增后端 `pyproject.toml`，建议 Python 3.12，使用应用项目模式，不急于把 `main/` 改造成可发布包。
2. 只声明 HTTP 包装器真实需要的轻量依赖，例如 FastAPI、Uvicorn、Pydantic、`python-multipart`、Starlette 兼容依赖和 `soundfile`。具体版本应从当前 `qwen3-tts` 包装器导入检查和接口测试中确认，不凭猜测复制模型环境依赖。
3. 将 `pytest`/Ruff 或现有 `unittest` 相关工具放入开发依赖组。当前仓库指南规定的是标准库 `unittest`，因此第一阶段保留 `python -m unittest discover -s tests -v` 兼容命令；若改用 pytest，必须同步更新指南和测试基线。
4. 新增 `.python-version` 和 `uv.lock`。提交 `uv.lock`，不提交 `.venv/`、模型、上传音频、运行缓存或供应商源码副本。
5. 主 API 和 MOSS-SoundEffect 已分别通过 `qwen3_tts`、`moss_soundEffect` uv 项目完成包装器导入/启动验证。
6. 默认仍保留现有 `CONDA_ENV=qwen3-tts` 路径，直到所有包装器的无模型健康检查通过。这样升级失败时可以立即回退，不影响 WebUI。

### 阶段 2：为模型 Conda 环境建立可复现边界

每个环境单独记录，不将模型包加入后端 `pyproject.toml`：

- `envs/qwen3-tts.yml`
- `envs/qwen3-voiceDesign.yml`
- MOSS VoiceGenerator 使用 `moss_voiceGenerator/pyproject.toml` 和 `uv.lock` 管理，不再维护 Conda 环境清单。
- `envs/Step-Audio-EditX.yml`
- `envs/voxcpm2.yml`
- LongCat-AudioDiT 使用 `LongCat_AudioDiT_3.5B_bf16/pyproject.toml` 和 `uv.lock`，旧 Conda 环境已删除。
- MOSS-SoundEffect 原 `moss-soundEffect` Conda 环境已删除；依赖由 `moss_soundEffect/pyproject.toml` 和 `uv.lock` 管理。
- Stable Audio 3 Medium 使用 `stable_audio_3_medium/pyproject.toml` 和 `uv.lock`；原 `stable_audio_3_medium` Conda 环境已删除。
- `envs/audio_eval.yml`

每份清单至少应包含：Python 版本、CUDA/PyTorch 约束、关键 pip 或源码安装来源、必要的系统工具、模型/Tokenizer 目录变量、离线/镜像变量、验证命令和已知 GPU 限制。模型权重只写下载/挂载位置，不写进 Git。

推荐使用以下治理方式：

1. 用 `conda env export --from-history` 或人工维护的最小 YAML 作为可读输入。
2. 对需要精确部署的环境，再生成不含机器绝对路径的 lock 文件或保留经过验证的 pip constraints。
3. 把 Flash Attention、vLLM、CUDA 版 PyTorch、官方上游源码等安装步骤留在对应环境指南，不让 uv 重新解析它们。
4. `start.sh` 继续使用 `conda run --no-capture-output -n <env>`，不依赖交互式 `conda activate`；升级只改善环境检查和错误提示。
5. 通过 `GET /v1/health` 先检查 Conda、模型目录、Tokenizer、worker 文件和 CUDA，再执行真实推理 smoke test。

### 阶段 3：整理 `scoring-for-TTS`

1. 先决定 `timbre-design` 是真实包还是历史占位配置。当前 `src/timbre_design` 和 `timbre_design.cli` 不存在，建议短期使用应用项目模式，移除或暂时停用指向不存在源码的 package/CLI 声明。
2. 按实际脚本导入补齐运行依赖，按用途拆分 `test`、`lint`、`eval` dependency group；基础 `uv sync` 不应下载 CUDA 模型栈。
3. 保留 `modelScript/` 中的每模型安装指南和各自 Conda 环境。评测工具只负责调度和检查，不负责将所有模型依赖合并。
4. 恢复或重写 `tests/` 引用的缺失 `tts-bench`、`cloneData`、Higgs 脚本和文档 fixture；每个缺失项要么回补权威文件，要么删除已失效测试并说明原因。
5. 在 `uv run python -m pytest -q` 和 `uv run ruff check .` 可重复执行后，再提交 `uv.lock`。

### 阶段 4：契约测试和渐进切换

至少建立以下无模型测试：

- 所有 API 模块可以在控制面环境导入，不加载模型权重、不初始化 CUDA。
- `GET /v1/health` 的 JSON 关键字段仍存在，缺少模型时返回可诊断状态而不是导入崩溃。
- `POST /v1/upload_audio`、`GET /v1/check/audio` 的文件名、SHA-256、大小和覆盖行为保持不变。
- 各 `/v2/synthesize` 对 `text`、`audio_path`、`prompt_text`、`backend`、`clone_mode`、`control_instruction` 和 `nonverbal_tags` 的兼容行为保持不变。
- Step-Audio-EditX 的 `/v1/step-audio-editx/edit` 保留 `prompt_audio`、`prompt_text`、`generated_text`、`edit_type`、`edit_info` 字段。
- SoundEffect 的 `/v1/generate` 和兼容的 `/v2/synthesize` 仍返回 `audio/wav`；请求仍支持 `prompt` 和 `seconds`。
- worker subprocess、超时、非零退出、空 WAV、取消和 GPU 锁释放都用 mock 测试，不在默认测试中调用 CUDA。
- `sfx_plan` 只能挂在 `dialogue` 上，不能变成独立 `type="sfx"`。每项继续保存 `prompt`、`prompt_en`、`duration_seconds`、锚点和偏移等字段。

切换顺序建议是：

```text
旧 start.sh + 旧包装器
        │  同一端口、同一请求 fixture
        ▼
uv 控制面包装器 + Conda worker
        │  逐服务健康检查与 mock 契约测试
        ▼
uv 控制面作为默认启动方式，旧 Conda 包装器保留为回退路径
```

## 七、必须保留的 WebUI 交互契约

升级内部目录或启动方式时，`TTS-Studio-WebUI` 不应被迫同步修改以下行为。

### 7.1 TTS 和音色设计

- 克隆服务继续提供 `GET /v1/check/audio`、`POST /v1/upload_audio` 和 `POST /v2/synthesize`。
- VoxCPM2 的 `clone_mode="ultimate"` 与 `clone_mode="controllable"` 仍然互斥：前者使用准确 `prompt_text`，后者使用 `control_instruction` 和可选 `nonverbal_tags`，不能把两个路径合并成一个含糊字段。
- `control_instruction` 不带外层括号；后端内部仍负责组装 `(control_instruction)[tag]正文`。
- LongCat 仍需要参考音频逐字对应的 `prompt_text`，并保持 CUDA、24 kHz 单声道和模型时长限制。
- Step-Audio-EditX 仍先上传 prompt 音频，再用固定 JSON 调用主服务的编辑接口；编辑结果不能覆盖原始台词资产。
- Qwen、MiMo、VoxCPM2 的音色设计路由继续分别为 `/v1/qwen/design`、`/v1/mimo/design`、`/v1/voxcpm2/design`。路径大小写兼容项也不能在迁移中删除。

### 7.2 SoundEffect

当前 WebUI 的关键事实是：

1. `8311` 是 MOSS-SoundEffect v2，使用中文 `sfx_plan.prompt`；`8313` 是 Stable Audio 3 Medium，使用同一事件的英文 `sfx_plan.prompt_en`。
2. 两者都调用 `POST /v1/generate`，请求体至少包含 `prompt` 和 `seconds`，成功响应是 WAV，而不是 JSON。
3. 每项音效计划属于对应 `dialogue.sfx_plan`，前端把 WAV 存入 IndexedDB，并通过 `audioAssetKey` 恢复、导出、导入和混音；不能改回工程级固定素材名称查找。
4. 不应输出独立 `type="sfx"`，也不应把 `sfx_plan` 音频 URL 写成永久服务文件路径。
5. Stable Audio 的英文提示词应是全英文，并以 `TrackType: SFX` 结束；旧工程缺少 `prompt_en` 时可以继续使用 MOSS，但不能把中文提示词静默发送给 Stable Audio。
6. 生成请求需要顺序执行；后端共享 GPU 锁和一请求一 worker 的现实限制不允许浏览器并发提交几十个音效。

### 7.3 对当前 8 条音效基线的判断

`soundEffectResult/resultDrama.json` 当前包含 8 个计划，集中在视频通话提示、玻璃球碎裂、挂断、关门和怪物嘶嘶声等显著事件。这个数量相对于 117 行原文是偏少的。以下判断合理且应作为提示词/契约测试的正例：

- “把一个落满灰尘的箱子拖到吊灯下面”：拖拽箱子是明确的木材/地面摩擦声。
- “他凑近嗅了嗅”：如果要表现动作，可生成短促、近距离的吸气/嗅闻声；不能把抽象的“闻起来像茉莉花”本身生成成音效。
- “敲响布罗迪家门……他敲到第三下才开门”：这是明确的三次敲门声，且应记录次数和节奏。

同时，不能因为“更全面”就给每个动作都生成声音。应区分“明确可听、对叙事有帮助”的动作和纯视觉/嗅觉/心理信息：拖拽、开盒、倒出、三次敲门、门把手猛拉、脚步/奔跑、门猛烈关合可以进入候选；“烟味”“看见火焰女人”“脸色惨白”不能直接变成声音。最终结果应由 LLM 计划加人工审核共同决定。

因此，现代化迁移应把 `sfx_plan` JSON 作为稳定 fixture，并增加覆盖率/忠实性检查，而不是在重构 API 时顺便改变提示词字段。

## 八、验收门槛

### 后端控制面

- `uv sync` 在无模型机器上成功，且不会下载任何权重。
- `uv run python -c "import fastapi, pydantic, uvicorn"` 成功。
- 所有包装器在无模型条件下可导入，健康端点能报告缺失模型/Conda/CUDA，而不是启动即崩溃。
- `uv run ruff check` 和格式检查只覆盖自有源码、测试和脚本；供应商目录有明确排除规则。
- `python -m unittest discover -s tests -v` 在后端仓库重新具备可执行的真实测试目录，且不需要 CUDA、模型或外部服务。

### 模型运行面

- 每个 worker 仍以既定 Conda 环境启动；环境名、模型目录、Tokenizer、源码路径都能由环境变量覆盖。
- 每个环境至少通过 import smoke test、`torch.cuda.is_available()` 检查和一个小型本地模型推理验证。
- GPU 锁在成功、异常、超时和取消后都能释放；worker 进程组不会残留。
- 不把 `.venv`、Conda 环境目录、模型权重、上传参考音频和生成 WAV 提交到任一仓库。

### WebUI 功能面

- 8300/8305/8306/8307/8308/8311/8313 的健康检查和既有端口不变。
- Qwen、VoxCPM2、LongCat、dots.tts-soar 的上传、克隆和 WAV 返回不变。
- 五个音色设计入口和 Step-Audio-EditX 编辑入口不变。
- MOSS/Stable Audio 的 `prompt`、`prompt_en`、`seconds` 和 `audio/wav` 契约不变。
- 旧工程能恢复没有 `prompt_en`、`audioAssetKey` 或新表演字段的历史数据；新工程能刷新、导出和导入生成的 SoundEffect WAV。
- `bash start.sh` 仍是可用的回退入口。

### 评测面

- `scoring-for-TTS` 的缺失测试资源和文档契约先处理完，再建立 uv 锁文件。
- `uv run python -m pytest -q` 的失败数量必须从当前基线清零，或每个保留失败都有明确的环境/硬件说明。
- Task 6–9 的冻结文本、评测顺序、VoxCPM2 环境和报告格式不能因包管理器迁移改变。
- 模型评测和无模型单元测试分开执行；不能为了让 CI 通过而跳过真实的环境预检。

## 九、风险、回滚和不应做的事

### 主要风险

- 直接把 API 包装器从 `qwen3-tts` 切到 `.venv` 后，某个隐式导入可能依赖当前 Conda 环境里的包。
- 生成 `uv.lock` 时把本地路径、vendor 目录或 GPU 包误当作公开 PyPI 依赖，导致其他机器无法同步。
- 对 API 文件执行全仓库自动格式化，改变导入顺序、路径解析、错误处理或 subprocess 生命周期。
- 重命名 Conda 环境或模型目录变量，导致现有 `start.sh`、安装指南和个人部署脚本失效。
- 只跑 mock 测试而没有真实 GPU canary，遗漏 CUDA、Flash Attention、Tokenizer 或模型权重问题。

### 回滚方式

1. 第一阶段只新增 `pyproject.toml`、lock、环境说明和测试，不删除现有启动路径。
2. `start.sh` 保留旧的 `CONDA_ENV` 分支，新增路径通过显式环境变量启用。
3. 每个服务单独切换和验证；任何一个模型失败，只回退该服务的包装器启动方式，不回滚其他已验证服务。
4. 在所有端点和 WebUI 工程恢复回归通过前，不删除历史安装指南、兼容字段、路径别名或旧配置变量。

### 明确不建议

- 不要把所有 TTS 模型、音效模型和评测模型塞进一个 `dependencies` 列表。
- 不要用 uv 取代 CUDA 驱动、Conda、官方上游安装步骤或模型权重管理。
- 不要修改端口、接口路径、响应 MIME 类型和 JSON 字段来“顺便清理旧代码”。
- 不要把 `sfx_plan` 改成独立时间轴类型，除非同时设计实时播放、离线混音、导入导出和中途续播的完整 schema 迁移。
- 不要删除 `storage/clone/`、`storage/timbre/` 或运行缓存来冒充项目升级；这些是运行数据/缓存治理问题。

## 十、最终建议和实施优先级

| 优先级 | 工作项 | 结果 |
| --- | --- | --- |
| P0 | 冻结端口、路由、请求/响应和 `sfx_plan` fixture；修复两个仓库当前测试资源缺失问题 | 有可信的迁移前基线 |
| P1 | 给后端补 `pyproject.toml`、`.python-version`、`uv.lock`、无模型 `tests/` 和 Ruff 配置 | 控制面可安装、可检查、可回归 |
| P1 | 给 `scoring-for-TTS` 修正 package/application 边界并生成自己的 lock | 评测项目可独立复现 |
| P1 | 为 11 个模型/评测 Conda 环境补清单和预检命令 | GPU 运行面可审查、可重建 |
| P2 | 让 `start.sh` 支持 uv 控制面与 Conda worker 的双路径 | 兼容切换、可回滚 |
| P2 | 增加 WebUI contract fixture、SoundEffect 队列和资产恢复测试 | 防止前端可见功能回归 |
| P3 | 在所有 canary 通过后，再考虑 `main/` 包结构、统一 CLI、类型检查和 CI 发布流程 | 降低后续重构风险 |

总体判断：升级的核心不是“把 Conda 换成 uv”，而是把轻量 API/评测控制面和重型 GPU 推理面明确分层。按这个边界实施，能够获得 `pyproject.toml`、`uv.lock`、Ruff 和可重复测试带来的收益，同时保留现有模型的 Python/CUDA 组合、GPU 锁、一次性 worker 和 WebUI 交互契约。

## 参考资料和现状证据

- 后端启动与环境路由：`start.sh`
- 后端接口和运行说明：`README.md`
- worker 生命周期：各模型目录内的 `runtime.py`/`worker.py`、`main/gpu_runtime.py`
- 统一请求模型：各模型目录内的 `synthesis_request.py`
- 主 API：`main/main.py`
- SoundEffect API：`moss_soundEffect/main.py`
- WebUI 接入契约：`../TTS-Studio-WebUI/docs/TTS-and-VoiceDesign接入.md`
- WebUI SoundEffect 客户端：`../TTS-Studio-WebUI/js/soundeffect-client.js`
- SoundEffect 输入/输出样本：`soundEffectResult/deepseekRequest.json`、`soundEffectResult/modifyDeepseekRequest.json`、`soundEffectResult/resultDrama.json`、`soundEffectResult/text.md`
- 评测项目配置：`../scoring-for-TTS/pyproject.toml`
- 评测项目规范：`../scoring-for-TTS/AGENTS.md`
