# 克隆音频建议

> 核验日期：2026-07-24。本文只把模型发布方的仓库、模型卡、正式文档和官方实现视为“官方来源”；社区经验不会被写成官方结论。

## 适用范围

本项目共有 7 个 TTS 参考音频克隆服务：

| 端口 | 模型 |
| --- | --- |
| `8300` | IndexTTS2 (`IndexTeam/IndexTTS-2`) |
| `8301` | dots.tts-base (`rednote-hilab/dots.tts-base`) |
| `8302` | LongCat-AudioDiT-1B (`meituan-longcat/LongCat-AudioDiT-1B`) |
| `8303` | MOSS-TTS-Local-Transformer-v1.5 |
| `8304` | OmniVoice |
| `8305` | Qwen3-TTS-12Hz-1.7B-Base |
| `8306` | VoxCPM2 |

`8311` 的 MOSS-SoundEffect v2.0 虽然也保留了 `/v2/synthesize` 别名，但它是文本生成声效接口，不接收参考音频，也不是 TTS 音色克隆模型，因此不属于本文范围。

## 一份音频兼容全部模型时怎么准备

官方没有给出一个适用于全部模型的统一规范。综合下文逐模型的明确建议和实现边界，本项目可采用以下**工程统一值**：

- 使用 **5～10 秒**的单人连续自然语音。这个范围同时落在 OmniVoice 官方的 3～10 秒、VoxCPM2 官方的 5～30 秒建议内，也不会触及 IndexTTS2 的 15 秒截断边界。它是本项目的交集型建议，不是所有模型共同发布的官方最优值。
- 优先保存为无损 WAV；作为跨模型母版可使用 **48 kHz、16-bit PCM、单声道**。各 worker 会按模型需要重采样，例如 LongCat 使用 24 kHz 单声道，VoxCPM2 编码端使用 16 kHz。不要为了升采样而伪造高频信息。
- 只保留一个说话人，尽量没有背景音乐、环境噪声、混响、削波、变声效果或重叠说话；剪掉大段首尾静音，但不要切掉起音和尾音。
- 参考音频应体现希望克隆的口音、情绪、语速和力度。模型会复制的不只是音色，也可能包括参考片段的韵律和口音。
- 除 IndexTTS2 外，均建议随音频提交**逐字准确的 `prompt_text`**。不要漏掉实际说出的词、语气词或重复，也不要写入音频中不存在的内容。
- 涉及跨语言克隆时，先用同语言参考做基线。OmniVoice 官方明确说明，跨语言生成可能带上参考语言的口音。

## 快速对照

| 模型 | 官方给出的时长信息 | 最佳官方条件 | 本项目建议 |
| --- | --- | --- | --- |
| IndexTTS2 | 文档未声明最优时长；官方实现只取前 15 秒 | 单一音色参考；关闭随机采样有利于克隆保真度 | 5～10 秒，最长不要超过 15 秒；无需 `prompt_text` |
| dots.tts-base | 官方评测使用约 3 秒参考；未声明“10 秒最优” | 参考音频 + 精确转写的 continuation 模式，官方标为 recommended / best SIM | 约 3～10 秒并提供准确 `prompt_text` |
| LongCat-AudioDiT-1B | 未声明参考音频最优区间；参考 + 生成总时长上限为 30 秒 | `prompt_audio` + 对应 `prompt_text`；官方克隆示例使用 APG | 5～10 秒并提供准确 `prompt_text`，避免长参考挤占生成预算 |
| MOSS-TTS Local v1.5 | 未声明最优区间；官方强调新版更能处理“长参考、短目标” | 参考音频可直接克隆；同时给转写可提高说话人相似度 | 5～10 秒并提供准确 `prompt_text`；不要套用 MOSS-TTSD 的 10～20 秒说法 |
| OmniVoice | **3～10 秒**；更长会变慢且可能降低克隆质量 | 短参考 + 转写；同语言参考可获得更标准的发音 | 严格控制在 3～10 秒，优先人工 `prompt_text` |
| Qwen3-TTS Base | 支持“3 秒级”快速克隆；未声明最优区间或上限 | `ref_audio` + `ref_text`；仅 x-vector 模式质量可能下降 | 5～10 秒并提供准确 `prompt_text` |
| VoxCPM2 | **5～30 秒**为实用范围；官方 cookbook 建议至少 5 秒 | 干净参考；同一片段同时用于 reference 和 prompt，并给精确转写，可获得最高相似度 | 通常 5～10 秒；提供准确 `prompt_text` 走 combined / Ultimate Cloning |

## 逐模型依据与建议

### 1. IndexTTS2（8300）

官方 README 的基础克隆调用只接收一个 `spk_audio_prompt`，不要求参考转写。官方实现会把音色参考读取后截到前 15 秒，再分别重采样到 22.05 kHz 和 16 kHz；因此超过 15 秒的后半段不会参与克隆。官方 README 还明确提示，启用随机采样会降低音色克隆保真度。

建议：

- 选择 5～10 秒信息密度高的片段，最多 15 秒，并确保最有代表性的内容位于开头。
- `8300 /v2/synthesize` 不消费 `prompt_text`，无需为 IndexTTS2 单独制作参考转写。
- 参考片段本身应带有所需情绪；官方还支持把音色参考和情感参考分开，但本项目的公共克隆接口没有单独的情感音频字段。

官方来源：[IndexTTS2 使用说明](https://github.com/index-tts/index-tts#-using-indextts2-in-python)、[官方 15 秒截断实现](https://github.com/index-tts/index-tts/blob/main/indextts/infer_v2.py#L334-L345)、[音色参考调用处](https://github.com/index-tts/index-tts/blob/main/indextts/infer_v2.py#L435-L445)。

### 2. dots.tts-base（8301）

官方把“参考音频 + 参考转写”的 continuation voice cloning 标为推荐方式和最佳 SIM，并要求 `prompt_text` 是参考音频的精确转写。只有参考音频、不提供转写时，会退化为 x-vector-only 克隆。模型卡说明音频 VAE 工作在 48 kHz 单声道；官方 Seed-TTS-Eval 的零样本结果使用约 3 秒参考音频。

需要特别澄清：截至核验日期，官方仓库和 `dots.tts-base` 模型卡**没有**写“参考音频控制在 10 秒内效果最好”。10 秒可以作为本项目的保守工程上限，但不能标为 dots.tts 的官方最优结论；官方直接可证的时长信息是“评测使用约 3 秒参考”。

建议：

- 优先选约 3～10 秒、边界完整的片段。
- 必须尽量提供与语音逐字一致的 `prompt_text`；转写不准时，宁可修正转写，不要把仅音频模式当作同质量替代。
- 使用无损音频母版；官方内部按 48 kHz 单声道建模。

官方来源：[dots.tts 官方仓库的克隆模式说明](https://github.com/studio-dots-ai/dots.tts#cli)、[dots.tts-base 模型卡](https://huggingface.co/rednote-hilab/dots.tts-base)、[约 3 秒参考的官方评测](https://huggingface.co/rednote-hilab/dots.tts-base#performance--dotstts-base)。

### 3. LongCat-AudioDiT-1B（8302）

官方克隆示例同时传入 `prompt_audio` 和 `prompt_text`，把“参考转写 + 目标文本”拼接后送入文本编码器。参考音频由官方加载函数转换为 24 kHz 单声道。官方克隆示例采用 APG guidance；模型配置的 `max_wav_duration` 为 30 秒，推理代码把参考和生成部分共同计入这个总时长预算。

官方没有给出独立的参考音频最优时长或噪声门槛。因此不能把社区常见的“3～15 秒最好”写成 LongCat 官方建议。

建议：

- 使用 5～10 秒片段，给目标语音留下充足的 30 秒总预算。
- 提供逐字准确的 `prompt_text`。本项目缺失转写时会调用本地 ASR，但人工校对结果通常更可靠。
- 输入可保留高质量母版；worker 会按官方方式转成 24 kHz 单声道。

官方来源：[LongCat-AudioDiT 克隆说明](https://github.com/meituan-longcat/LongCat-AudioDiT#2-voice-cloning-with-prompt-audio)、[官方推理实现](https://github.com/meituan-longcat/LongCat-AudioDiT/blob/main/inference.py#L56-L108)、[模型配置](https://huggingface.co/meituan-longcat/LongCat-AudioDiT-1B/blob/main/config.json)。

### 4. MOSS-TTS-Local-Transformer-v1.5（8303）

官方模型卡支持直接把参考音频作为 `reference` 克隆；其服务文档说明，同时提供参考片段和转写可获得更好的说话人相似度。v1.5 的音频 tokenizer 支持原生 48 kHz 立体声输入输出，并明确宣称比 v1.0 更稳定地处理“参考音频远长于目标文本”的场景。已知目标语言时，官方建议显式提供 language tag。

官方没有为 `MOSS-TTS-Local-Transformer-v1.5` 公布参考时长最佳区间。MOSS-TTSD 文档中的“10～20 秒”针对另一套双人对话模型，不能直接移植到本模型。

建议：

- 跨模型共用时仍用 5～10 秒；只使用 MOSS v1.5 时可以测试更长参考，不必因其他模型的限制认定其一定会变差。
- 提供准确 `prompt_text`，让本项目走 continuation 克隆；不提供时仍可使用 reference-only 克隆。
- 已知目标语言时传入正确 `language`；48 kHz 无损输入最贴近官方 tokenizer 的原生规格。

官方来源：[MOSS-TTS-Local-Transformer-v1.5 模型卡](https://huggingface.co/OpenMOSS-Team/MOSS-TTS-Local-Transformer-v1.5)、[v1.5 克隆改进与音频规格](https://huggingface.co/OpenMOSS-Team/MOSS-TTS-Local-Transformer-v1.5#model-card-for-moss-tts-local-transformer-v15)、[参考片段和转写说明](https://huggingface.co/OpenMOSS-Team/MOSS-TTS-Local-Transformer-v1.5#voice-cloning)。

### 5. OmniVoice（8304）

这是官方建议最明确的模型：使用 **3～10 秒**参考音频；更长的音频会拖慢推理，并可能降低克隆质量。为了标准发音，参考音频应与目标语音使用相同语言；跨语言克隆会带入参考语言的口音。`ref_text` 可以人工提供，也可以省略后由 Whisper 自动转写。默认 `preprocess_prompt=True` 会去除参考音频中的长静音，并在参考文本末尾补标点。

建议：

- 严格选 3～10 秒，优先 5～10 秒的清晰连续片段。
- 优先人工提供准确 `prompt_text`，避免自动转写错误进入克隆上下文。
- 追求标准发音时使用与目标文本同语言的参考；刻意保留口音时再做跨语言克隆。
- 保持 `preprocess_prompt` 开启，除非已确认关闭它更符合特定素材。

本项目默认用 `OMNIVOICE_MAX_REFERENCE_SECONDS=10.0` 在内存中限制参考时长，与官方 3～10 秒建议一致。

官方来源：[OmniVoice Voice Cloning 与 Tips](https://github.com/k2-fsa/OmniVoice#voice-cloning)、[官方生成参数：参考预处理](https://github.com/k2-fsa/OmniVoice/blob/master/docs/generation-parameters.md#prepost-processing)、[额外克隆提示](https://github.com/k2-fsa/OmniVoice/blob/master/docs/tips.md)。

### 6. Qwen3-TTS-12Hz-1.7B-Base（8305）

官方将 Base 模型描述为具备“3 秒级快速音色克隆”能力。标准克隆方式同时提供 `ref_audio` 和对应 `ref_text`；如果启用 `x_vector_only_mode=True`，可以不传转写，但官方明确警告克隆质量可能下降。官方本地模型文档没有发布参考音频的最优时长区间或最大时长。

建议：

- 使用 5～10 秒清晰片段；3 秒代表官方声明的短参考音频克隆能力，不等于官方宣称的唯一最优时长。
- 提供逐字准确的 `prompt_text`，并保持 `x_vector_only=false`；本项目只有缺失转写时才回退到仅 x-vector 模式。
- 不要把阿里云托管的 Qwen-Audio-TTS / Realtime 音频要求直接当成本地 `Qwen3-TTS-12Hz-1.7B-Base` 的要求，两者不是同一个模型接口。

官方来源：[Qwen3-TTS Base 模型表与 Voice Clone](https://github.com/QwenLM/Qwen3-TTS#voice-clone)、[Qwen3-TTS Base 模型卡](https://huggingface.co/Qwen/Qwen3-TTS-12Hz-1.7B-Base#voice-clone)、[Qwen 官方 3 秒级克隆说明](https://qwen.ai/blog?id=qwen3-tts-vc-voicedesign)。

### 7. VoxCPM2（8306）

VoxCPM2 官方使用指南给出的参考音频实用范围是 **5～30 秒**，并说明更干净的音频通常能更好地保留音色；官方 cookbook 进一步建议至少 5 秒。支持 torchaudio 可读的 WAV、FLAC、MP3 等格式，VoxCPM2 编码端工作在 16 kHz，解码输出为 48 kHz。

VoxCPM2 支持两种与本项目有关的方式：只传 `reference_wav_path` 即可稳定克隆音色，无需转写；如果同一片段同时作为 `reference_wav_path` 和 `prompt_wav_path`，并提供精确 `prompt_text`，官方模型卡称其为最高相似度的 Ultimate Cloning，架构文档也说明 combined 模式比单独 reference 或 continuation 略有提升。

建议：

- 一般使用 5～10 秒；复杂音色可逐步增加，但不超过官方 30 秒实用范围。
- 提供准确 `prompt_text`，使本项目同时传 reference 和 prompt；省略时会使用 reference-only，仍然有效但不是最高相似度路径。
- 优先在录制阶段得到干净音频。官方说明 denoiser 适合有噪参考，但它运行在 16 kHz，可能轻微改变音色；已经干净的参考更适合关闭降噪以保留原始特征。

官方来源：[VoxCPM2 参考音频使用指南](https://voxcpm.readthedocs.io/en/latest/usage_guide.html#reference-audio)、[官方 Voice Chef cookbook](https://voxcpm.readthedocs.io/en/latest/cookbook.html#cloning-a-masterpiece-voice-cloning)、[VoxCPM2 模式与 combined 模式说明](https://voxcpm.readthedocs.io/en/latest/models/voxcpm2.html#isolated-reference-audio-channel)、[VoxCPM2 模型卡](https://huggingface.co/openbmb/VoxCPM2#ultimate-cloning)。

## 上传前检查清单

- [ ] 已获得说话人授权，生成内容会明确标识为 AI 合成。
- [ ] 只有一个说话人，没有重叠人声、背景音乐或明显环境噪声。
- [ ] 音频没有削波、爆音、强混响、电子变声或过度降噪伪影。
- [ ] 共用素材时长在 5～10 秒；IndexTTS2 不超过 15 秒，OmniVoice 不超过 10 秒，VoxCPM2 在 5～30 秒内。
- [ ] 首尾没有大段静音，也没有截断音节。
- [ ] 片段的语言、口音、情绪和语速符合目标角色。
- [ ] 除 IndexTTS2 外，已提供与实际语音逐字一致的 `prompt_text`。
- [ ] 已用同一片段做一轮短句 A/B 测试；若结果不稳定，优先更换参考片段或校正转写，再调整推理参数。

## 结论

如果只保留一条规则：为本项目准备一段**经授权、5～10 秒、单人、清晰、连续、无背景音乐的无损语音，并附逐字准确转写**。IndexTTS2 会忽略转写；其余六个模型都能从准确转写中直接受益或进入官方更高保真的克隆路径。模型间最重要的差异是：OmniVoice 明确限制在 3～10 秒，IndexTTS2 官方实现只取前 15 秒，VoxCPM2 明确建议 5～30 秒，而 dots.tts 当前官方资料只证明约 3 秒评测和 continuation + 精确转写最佳，不能把“10 秒内最好”冒充其官方结论。
